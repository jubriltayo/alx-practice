#include "main.h"

int _strlen(char *s)
{
  int count;
  
  for (count = 0; *s != '\0'; ++s)
      count++;
      
  return count;
}

OR

int _strlen(char *s)
{
    int i;

    while (s[i] != '\0')
    {
        s[i];
        i++;
    }
    return (i);
}
